# Webhook URL из Bitrix24 для отправки лидов в CRM.
# Без этой переменной API /api/leads работает в mock-режиме (логирует в консоль, не отправляет данные).
# Формат: https://your-domain.bitrix24.ru/rest/USER_ID/WEBHOOK_TOKEN/crm.deal.add.json
BITRIX24_WEBHOOK_URL=
