name: CMS schema audit — production

on:
  workflow_dispatch:
    inputs:
      confirmation:
        description: "Введите AUDIT_PRODUCTION"
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: cms-production-schema-audit
  cancel-in-progress: false

jobs:
  audit:
    name: Read-only Payload schema audit
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 20

    steps:
      - name: Validate manual confirmation
        shell: bash
        env:
          PRODUCTION_AUDIT_CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          if [ "$PRODUCTION_AUDIT_CONFIRMATION" != "AUDIT_PRODUCTION" ]; then
            echo "Incorrect production audit confirmation."
            exit 1
          fi

      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 22
          cache: npm
          package-manager-cache: false

      - run: npm ci

      - name: Read-only production schema audit
        env:
          DATABASE_URL_UNPOOLED: ${{ secrets.DATABASE_URL_UNPOOLED }}
        run: npm run cms:migrate:audit
