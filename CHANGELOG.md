# CHANGELOG

## 2026-05-12 — CMS-driven content migration begins

- Added `CMS_CONTENT_ARCHITECTURE.md` with the current hardcoded/CMS content map and incremental migration strategy.
- Connected frontend contacts to Payload global `contacts` through `lib/cms/contacts.ts`.
- Kept production-safe fallbacks for phones, email, address, work hours and social links.
- Homepage remains visually unchanged; managers can now update contact data from CMS when the global is populated.

## 2026-05-12 — Authenticated admin smoke tooling

- Added read-only `cms:admin-smoke` validation for logged-in Payload admin checks with env-based credentials.
- The smoke script validates login, `/api/users/me`, admin shell, light theme, key collection/global routes and basic response timings.
- The smoke script now supports local `.env.local` for the allowed admin smoke keys; shell env still has priority.
- The smoke script can validate protected Preview deployments through Vercel CLI transport without printing credentials.
- Confirmed Preview and Production currently share DB/storage/auth env, so automatic admin creation, media upload and write persistence checks are intentionally blocked.
- Runtime deployment validator now treats `lead-management` as a required global.

## 2026-05-12 — CMS admin UX architecture

- Admin перестроен в бизнес-структуру: главная страница, каталог, калькулятор, компания, медиа, заявки, пользователи.
- Добавлен dashboard "Центр управления сайтом и каталогом" с быстрыми переходами для менеджеров.
- Улучшены русские labels, descriptions, placeholders и list views для collections/globals.
- Поле пользователя "Имя" больше не используется как ФИО: добавлены имя, фамилия, отображаемое имя и должность.
- Медиа-библиотека получила тип материала, область использования и редакторские заметки.
- Добавлен безопасный global "Заявки: формы и интеграции" без хранения секретов.
- Hero background video заменён на новый оптимизированный MP4 из предоставленного `IMG_4120.MOV`.
- Зафиксированы performance findings: основная задержка admin похожа на Vercel/Neon cold start; списки облегчены через default columns и pagination.

## 2026-05-12 — Production deploy Payload CMS

- `feat/payload-cms` fast-forward merged в `main` и выведен в production: `https://kbparus-metal-storage.vercel.app`.
- Production validation прошла: `/api/health` ok, `cms.ok=true`, globals `contacts` и `home-content` читаются, `/admin` на русском и в светлой теме, публичные страницы работают, `/api/users` закрыт без авторизации.
- Preview Telegram tests доставлены через настроенный Telegram-канал: заявка конфигуратора включает выбранные параметры, обычная контактная заявка остаётся короткой.
- Warning: Bitrix24 env не настроен; это не блокирует этап до подготовки интеграции Bitrix24.

## 2026-05-12 — Русская админка Payload и контраст

### Что изменилось

- Payload admin переведён на русский через штатный пакет `@payloadcms/translations`.
- Зафиксирована светлая тема админки, чтобы менеджеры видели стабильный интерфейс.
- Добавлен CSS-reset для `/admin`, который перекрывает глобальные стили публичного сайта и исправляет контраст текста, форм, кнопок, навигации и upload UI.

### Проверки

- `npm run lint`
- `npm run test`
- `npm run cms:generate-importmap`
- `npm run build`
- Preview deploy: `/api/health` вернул `status: "ok"`, `/admin/create-first-user` рендерит русские строки.

## 2026-05-12 — Production validation tooling

### Что изменилось

- Добавлен недеструктивный runtime-валидатор `scripts/cms/validate-deployment.mjs`.
- Добавлен npm-скрипт `cms:validate-deployment`.
- Добавлен документ `PRODUCTION_VALIDATION.md` с матрицей проверок: admin auth, globals persistence, uploads/blob storage, rebuild consistency, env vars.

### Что проверять

- `npm run cms:validate-deployment -- <preview-url>`
- После cutover: `npm run cms:validate-deployment -- https://kbparus-metal-storage.vercel.app --production`
- Ручные проверки admin auth и upload остаются обязательными, потому что они требуют авторизованной сессии и записи файла.

## 2026-05-11 — Payload schema globals verification

### Что изменилось

- `scripts/cms/push-schema.ts` теперь явно вызывает Drizzle schema push через `@payloadcms/drizzle`, а не полагается на ленивый `push: true`, который Payload не выполняет при `NODE_ENV=production`.
- После schema push скрипт проверяет, что globals `contacts` и `home-content` реально читаются.
- `/api/health` теперь проверяет не только коллекции CMS, но и доступность globals. Если таблицы globals не созданы или не читаются, health возвращает `status: "degraded"`, а не ложный `ok`.
- README уточняет, что build должен падать до публикации, если глобальные таблицы Payload не готовы.

### Затронутые файлы

- `scripts/cms/push-schema.ts`
- `app/api/health/route.ts`
- `package.json`
- `package-lock.json`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Vercel build: `cms:push-schema` должен вывести чтение globals `contacts` и `home-content`.
- После deploy `/api/health` должен вернуть `status: "ok"` и `components.cms.globalNames`.
- `/admin` должен открывать экран `Create First User`.

## 2026-05-11 — Payload importMap generation стабилизирован

### Что изменилось

- Исправлена текущая причина падения Vercel build на `payload generate:importmap`: проект приведён к ESM-режиму через `"type": "module"` в `package.json`, как в официальных Payload templates.
- Extensionless imports в `payload.config.ts` сохранены: это штатный паттерн Payload, проблема была не в отсутствии `.ts` у импортов коллекций.
- `payload.config.ts` теперь явно указывает Payload, куда писать importMap: `app/(payload)/admin/importMap.ts`. Это исключает расхождение между сгенерированным `importMap.js` и импортируемым админкой `importMap.ts`.
- `scripts/cms/safe-generate-importmap.mjs` теперь запускает официальный Payload CLI на всех платформах и валит build при ошибке генерации. Ручной importMap не используется как финальное решение.
- `scripts/cms/check.mjs` проверяет реальные активные admin-компоненты importMap: Vercel Blob upload handler и Payload RSC components. Lexical RSC entries требуются только если в схемах реально есть `richText` поля.
- `scripts/cms/push-schema.mjs` переведён на прямой запуск `tsx` через Node без `npx` и shell-зависимостей для production-сборки на Linux/Vercel.

### Затронутые файлы

- `package.json`
- `payload.config.ts`
- `app/(payload)/admin/importMap.ts`
- `scripts/cms/check.mjs`
- `scripts/cms/safe-generate-importmap.mjs`
- `scripts/cms/push-schema.mjs`
- `README.md`
- `CMS_SETUP.md`
- `CHANGELOG.md`

### Что проверить

- `npm run cms:generate-importmap` проходит и не создаёт отдельный `importMap.js`.
- `npm run cms:check` проходит при заданных `PAYLOAD_SECRET`, `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `BLOB_READ_WRITE_TOKEN`.
- `npm run vercel-build` доходит до `next build`.
- В Vercel Build Logs есть успешные шаги `payload generate:importmap` и schema push.
- После deploy `/api/health` возвращает `status: "ok"`, а `/admin` открывает экран `Create First User`.

## 2026-05-08 — Catalog image interaction cleanup and video loop trim

### Что изменилось

- В каталоге главной убран хаотичный hover с дублированием изображения на фоне: теперь фото не становится прозрачным и не перекрывается размытой копией.
- Клик по изображению категории ведет внутрь категории.
- Для крупного просмотра в каталоге оставлена отдельная кнопка `Увеличить`; модальное окно открывается с анимацией, без внутренней прокрутки по картинке и с кнопкой перехода в категорию.
- Градиенты в карточках каталога теперь работают только как аккуратные верхние/нижние переходы фона и не портят качество самого изображения.
- Текстовые зоны карточек каталога выровнены по единому ритму: заголовки и CTA больше не начинают "плясать" на разных уровнях.
- Внутри страницы категории изображение раздела тоже можно открыть крупнее через кнопку `Увеличить`.
- Обновлено изображение 7-й категории: `Шкаф Инлокер`.
- Hero-видео дополнительно обрезано на 30 секунд в конце, чтобы зацикливание начиналось раньше и без лишнего хвоста.

### Затронутые файлы

- `components/CatalogGrid.tsx`
- `components/ImageLightbox.tsx`
- `components/LinePageStyles.tsx`
- `components/ProductGallery.tsx`
- `app/catalog/[id]/page.tsx`
- `public/assets/images/catalog/07-inlocker.png`
- `public/assets/videos/metal-storage-hero-trimmed.mp4`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На главной фото категории не мутнеет и не дублируется при наведении.
- По клику на фото категории происходит переход в раздел.
- Кнопка `Увеличить` открывает красивое модальное окно, а не переход.
- В модальном окне не нужно скроллить изображение в стороны или вниз, чтобы рассмотреть продукт.
- 7-я категория показывает новую картинку.
- Hero-видео зацикливается после укороченной версии ролика.

## 2026-05-08 — Hero video and large catalog visuals

### Что изменилось

- В hero добавлено локальное фоновое видео системы хранения листового металла: первые 8 секунд исходника обрезаны, ролик крутится без звука, затемнен и использует старый hero-визуал как fallback.
- Каталог главной вынесен в `components/CatalogGrid.tsx` и переделан под крупные продуктовые фото с минимумом текста.
- По клику на изображение категории открывается большой просмотр, чтобы можно было рассмотреть оборудование.
- Обновлена картинка 5-й категории на предоставленный файл `5.png`.
- Убран эффект “квадратная картинка вставлена в карточку”: для каталога, ассортимента внутри категории, галереи товара и превью калькулятора добавлены общий фон, blur/backdrop и крупные product-visual зоны.
- В товарной галерее добавлен крупный просмотр активного изображения.
- Добавлена dev-зависимость `ffmpeg-static` для локальной подготовки видеоассетов.

### Затронутые файлы

- `app/page.tsx`
- `components/CatalogGrid.tsx`
- `components/Calculator.tsx`
- `components/LinePageStyles.tsx`
- `components/ProductGallery.tsx`
- `data/storageSystems/excelCatalog.ts`
- `public/assets/images/catalog/05-carousel-vertical-module.png`
- `public/assets/videos/metal-storage-hero-trimmed.mp4`
- `package.json`
- `package-lock.json`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- Hero на production показывает фоновое видео, а не только статичную фотографию.
- Видео начинается после первых 8 секунд исходного ролика, крутится без звука и не мешает читаемости заголовка.
- В каталоге главной фото категорий стали крупнее, а картинка 5-й категории обновилась.
- Клик по фото категории открывает большой просмотр, а клик по текстовой части ведет внутрь категории.
- Внутри категории и на товарной странице изображения выглядят как часть карточки, без грубой квадратной рамки.
- Калькулятор продолжает работать, а превью оборудования не выглядит вставленной маленькой картинкой.

## 2026-05-08 — Category images update

### Что изменилось

- Добавлены локальные изображения для категорий 2, 4-17.
- Обновлены привязки изображений в каталоге главной страницы.
- Теперь все 17 категорий каталога используют локальные изображения.

### Затронутые файлы

- `data/storageSystems/excelCatalog.ts`
- `public/assets/images/catalog/*`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На главной в каталоге отображаются новые картинки категорий.
- Категории 1 и 3 остались с уже добавленными изображениями.
- Категория 11 отображает новое изображение фронтальных паллетных стеллажей.

## 2026-05-08 — Product page polish and product content

### Что изменилось

- Увеличена визуальная шапка: логотип, навигационные кнопки, иконки связи и телефоны стали крупнее.
- На странице товара добавлен отдельный верхний заголовочный блок, чтобы название товара не прижималось к хлебным крошкам и сразу читалось как главный смысл страницы.
- Верхняя навигация на страницах категорий и товаров доработана в более чистом стиле без тяжёлой подложки.
- В карточках ассортимента первой категории убрана плашка `Товар`.
- Названия товаров, описания, цена и CTA в карточках ассортимента выровнены по единой сетке, чтобы блоки не “плясали”.
- Кнопка `Связаться с инженером` на странице категории теперь ведёт к форме на этой же странице, а не перекидывает пользователя на главную.
- Для 4 пилотных товаров первой категории добавлены более предметные описания, сценарии применения и характеристики по открытым данным рынка. Публичные ссылки-источники на сайте не выводятся.

### Затронутые файлы

- `app/catalog/[id]/page.tsx`
- `app/catalog/[id]/[productId]/page.tsx`
- `components/ProductGallery.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/catalogDepth.ts`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На странице товара сверху видно название товара, и оно не прижато к навигационной строке.
- На странице `/catalog/auto-sheet-metal` карточки товаров имеют одинаковый ритм: название, описание, цена и переход выровнены.
- В карточках ассортимента нет плашки `Товар`.
- Кнопка `Связаться с инженером` на странице категории переводит к форме текущей страницы.
- Шапка стала крупнее и читается на desktop/tablet/mobile.

## 2026-05-07 — Product galleries and header refinement

### Что изменилось

- На страницах 4 пилотных товаров добавлена галерея изображений: большое фото, стрелки, счетчик и миниатюры.
- Фотографии товаров перенесены в локальные ассеты `public/assets/images/products/auto-sheet-metal/`.
- Главная шапка стилизована ближе к `линииокраски.рф`: без общей подложки, поверх hero, с отдельными кнопками.
- Навигация в правом верхнем углу страниц категорий и товаров сделана читаемой и оформлена как набор кнопок.
- В калькуляторе усилен контраст блока быстрых подсказок: "Не знаете, что выбрать?", "Храним трубы и профиль" и похожие тексты стали читаемыми.

### Затронутые файлы

- `app/catalog/[id]/[productId]/page.tsx`
- `components/ProductGallery.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/catalogDepth.ts`
- `public/assets/images/products/auto-sheet-metal/*`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На страницах товаров Compact, Logic, Spider и Cross листаются фотографии.
- На страницах категорий и товаров навигация читается и выглядит как часть сайта.
- В главной шапке нет общей светлой подложки.
- В калькуляторе читаются быстрые подсказки на первом шаге.

## 2026-05-07 — Contrast fixes and footer polish

### Что изменилось

- Исправлена читаемость заголовка в блоке до/после склада.
- Кнопка "Проложить маршрут" закреплена в оранжевой CTA-стилистике с белым текстом.
- Футер переделан в более стильный темный индустриальный блок с карточками навигации и контактов.

### Затронутые файлы

- `components/LinePageStyles.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- В блоке до/после читается заголовок "Из металла на полу в управляемую систему хранения".
- В контактах кнопка маршрута выглядит как часть дизайн-системы.
- Футер визуально совпадает с уровнем главной страницы.

## 2026-05-07 — Light main header

### Что изменилось

- Шапка главной переведена в светлый стиль, ближе к страницам каталога.
- Телефоны в шапке сделаны темными в обычном состоянии, чтобы они читались на светлом фоне.
- Логотип теперь ведет на главную страницу `/`.

### Затронутые файлы

- `components/BrandMark.tsx`
- `components/LinePageStyles.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На главной шапка светлая, а номера телефонов читаются.
- Наведение на телефон по-прежнему выделяет конкретный номер.
- Клик по логотипу ведет на главную.

## 2026-05-07 — Pilot category depth and product pages

### Что изменилось

- Для первой категории `Автоматизированные системы для хранения листового металла` добавлена пилотная глубина каталога.
- Со страницы первой категории убраны фильтры и промежуточные подкатегории.
- Вместо тестовых товаров добавлены 4 позиции со старого сайта: Compact 3000x1500, Logic, Spider и Cross.
- Карточки товаров ведут внутрь обычных товарных страниц без конфигуратора; конфигуратор будет использоваться только для тех товаров, где он действительно нужен.
- В данных зафиксирована будущая модель админки: товар может быть обычной карточкой или карточкой с привязанным профилем калькулятора.
- Для будущей админки добавлен режим цены товара: “Цена по запросу” или конкретная фиксированная цена.
- Данные подкатегорий и товаров вынесены в `data/storageSystems/catalogDepth.ts`, чтобы позже перенести их в админку/CMS.
- Остальные 16 категорий оставлены в прежнем режиме, пока не будет утвержден пилотный шаблон.

### Затронутые файлы

- `app/catalog/[id]/page.tsx`
- `app/catalog/[id]/[productId]/page.tsx`
- `components/ProductConfigurator.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/catalogDepth.ts`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `/catalog/auto-sheet-metal` показывает подкатегории и ассортимент.
- Клик по карточке товара ведет внутрь товара.
- `/catalog/auto-sheet-metal/compact-3000x1500` открывает товар Compact 3000x1500.
- `/catalog/auto-sheet-metal/logic-sheet-metal-storage` открывает товар Logic.
- `/catalog/auto-sheet-metal/spider-sheet-metal-storage` открывает товар Spider.
- `/catalog/auto-sheet-metal/cross-sheet-metal-storage` открывает товар Cross.
- Остальные категории каталога работают как раньше.

## 2026-05-07 — Hero breathing room and calculator guidance

### Что изменилось

- В hero на главной убраны все элементы между заголовком `Системы хранения металла` и блоком метрик: экран стал свободнее, спокойнее и ближе к референсу с крупной промышленной шапкой.
- В калькулятор добавлен быстрый старт по задаче клиента: листовой металл, трубы/профиль или выкатные кассеты.
- На шаге параметров добавлена визуальная подсказка по габаритам ДхШхВ, чтобы клиент понимал, что означают длина, ширина и высота.
- Часть вопросов в калькуляторе переписана на более понятный клиентский язык.
- В финальном результате добавлен блок “что клиент получает после заявки”: схема под склад, проверка нагрузки, подбор исполнения и предложение от инженера.

### Затронутые файлы

- `app/page.tsx`
- `components/Calculator.tsx`
- `components/LinePageStyles.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- На первом экране после заголовка сразу идут метрики, без промежуточных кнопок и текстовых плашек.
- Быстрый старт в калькуляторе подставляет подходящий тип системы и переводит клиента к параметрам.
- Подсказка ДхШхВ читается на desktop, планшете и мобильной версии.
- Расчет по-прежнему отображается в формате `от ... ₽`.

## 2026-05-07 — Premium calculator configurator pass

### Что изменилось

- Калькулятор переработан как главный продуктовый конфигуратор сайта: понятнее заголовок, сильнее визуальная иерархия, больше разделения по блокам.
- Названия оборудования в калькуляторе приведены к клиентскому языку сайта: например, `Автоматический стеллаж листового металла`, без внутренних сокращений.
- В карточки калькулятора добавлены реальные изображения из каталога там, где они уже есть.
- Убрана непонятная `Схема уровней` из правой панели живого расчета.
- Правая панель заменена на понятный summary: выбранная система, картинка, ключевые параметры, стартовая стоимость, количество опций и расчетный вес.
- Второй шаг сгруппирован по смыслу: габариты, нагрузка и вместимость, опции, объект и условия работы.
- Поля города и комментария сделаны читаемыми: темный текст на светлом фоне, понятные подписи и клиентские placeholder-подсказки.
- Третий шаг результата разделен на блоки: рекомендация, предварительная стоимость, выбранная конфигурация и что проверит инженер.
- Публичная цена теперь округляется до тысяч рублей: например, `34 428 563 ₽` отображается как `34 429 000 ₽`.
- В шапку добавлены реальные SVG-иконки Telegram и MAX из локальных ассетов.
- Добавлен отдельный баннер-переход на сайт `линииокраски.рф` с предоставленным оригинальным PNG и кнопкой поверх.
- Стилизован системный scrollbar: темная дорожка, оранжевый ползунок и hover-подсветка.

### Затронутые файлы

- `components/Calculator.tsx`
- `app/page.tsx`
- `components/LinePageStyles.tsx`
- `lib/calculator/format.ts`
- `tests/calculator.test.ts`
- `public/assets/icons/max.svg`
- `public/assets/icons/telegram.svg`
- `public/assets/images/kbparus-coating-lines-banner.png`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- В калькуляторе нет внутренних сокращений и упоминаний Excel.
- При вводе города и комментария текст хорошо виден.
- В правой панели нет `Схемы уровней`.
- Цена отображается в формате `от ... ₽` и округляется до тысяч.

## 2026-05-07 — Calculator contrast, motion and contact polish

### Что изменилось

- Исправлен контраст калькулятора: заголовок, параметры второго шага и результат третьего шага теперь явно заданы темным цветом на светлом фоне.
- Добавлены дополнительные микроанимации: перелив CTA, hover-glint карточек, плавная подсветка параметров калькулятора, hover-эффекты карты и выделение телефонов.
- Телефоны в шапке увеличены и получили отдельную hover-анимацию для выбранного номера.
- География больше не ограничивает формулировку Россией: добавлена подача «по России и за ее пределы».
- Баннер на основной сайт заменен на предоставленный визуал `kbparus-cnc-banner.png`, поверх оставлена только кнопка перехода на `kbparus.ru`.
- Партнеры временно заменены на аккуратные заглушки под будущие логотипы из админки.
- Ответы FAQ переведены на более контрастный светлый фон.
- Карта в контактах снова центрируется по ширине контента.
- Добавлена кнопка «Проложить маршрут» с переходом в Яндекс.Карты.
- В блок «О компании» добавлены иконки и визуальная графика метрик.
- Favicon настроен на логотип КБ Парус через metadata.
- Добавлена Open Graph-картинка-заглушка для предпросмотра ссылки в Telegram и других мессенджерах.
- Доработана адаптивность: отдельные правила для планшетов, компактная мобильная шапка и более удобная сетка каталога.
- FAQ-блок визуально усилен: добавлен инженерный фон, стеклянная шапка, акцентные линии и плавное появление ответов.
- Кнопка «Проложить маршрут» приведена к оранжевому CTA-стилю сайта, а ссылка маршрута уточнена до адреса КБ Парус.
- На мобильной версии закрыт dropdown каталога в шапке и уплотнена сетка карточек каталога до 2 колонок.
- Фото в карточках каталога интегрированы через размытый фон, чтобы не выглядели квадратной вставкой.
- Добавлены декоративные инженерные узоры по секциям.
- Блок преимуществ переделан в более уникальный industrial-стиль с разными формами карточек.
- В FAQ добавлен поиск по вопросам с анимированной фильтрацией.
- Нижний блок контактов обновлен в единой дизайн-системе.
- Калькулятор перенесен третьим блоком сразу после каталога; остальные блоки выстроены по логике: материалы, до/после, преимущества, кейсы, география, отзывы, компания, процесс, FAQ и контакты.

### Затронутые файлы

- `app/page.tsx`
- `app/layout.tsx`
- `app/opengraph-image.tsx`
- `components/LinePageStyles.tsx`
- `public/assets/images/kbparus-cnc-banner.png`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- В калькуляторе читаются заголовок, параметры и результат.
- При наведении на телефон подсвечивается конкретный номер.
- Баннер `kbparus.ru` отображается предоставленным изображением и содержит кнопку поверх.
- FAQ-ответы читаются на светлом фоне.
- Карта контактов находится по центру, кнопка маршрута открывает Яндекс.Карты.

## 2026-05-07 — Wide sections and mid-page product polish

### Что изменилось

- Основные блоки главной растянуты до ширины каталога, чтобы страница воспринималась цельнее.
- В каталоге заменены спорные метрики `4 раздела с фото` и `Клик по карточке` на более полезные: подбор по задаче и статус добавления фото.
- Вторая категория каталога переведена в placeholder-режим до появления правильного изображения.
- Блок «Что можно хранить» получил крупные карточки с промышленными placeholder-визуалами вместо случайных временных фото.
- Блок «До/после склада» перестроен как широкое визуальное сравнение без маленьких вложенных плашек.
- Калькулятор визуально усилен: повышен контраст текстов, расширена рабочая зона, улучшена правая панель живого расчета.
- Сломанный CTA-баннер после калькулятора выровнен, чтобы кнопка не накладывалась на текст.
- Дублирующий баннер «Поможем подобрать подходящее оборудование» удален.
- Кейсы переписаны как будущие карточки выполненных работ: компания, задача, результат и фото.
- Для кейсов, отзывов и партнеров добавлены рабочие кнопки прокрутки слайдера.
- География поставок заменена на Яндекс.Карту со списком городов и направлений.
- Партнеры переведены из текстовых плашек в logo-style карточки, которые позже можно заменить реальными логотипами.
- Подкорректированы преимущества, 3 шага и нижняя форма связи.

### Затронутые файлы

- `app/page.tsx`
- `components/LinePageStyles.tsx`
- `components/SliderControls.tsx`
- `data/storageSystems/excelCatalog.ts`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Все секции ниже каталога визуально совпадают с шириной каталога.
- Вторая категория каталога показывает заглушку, а не неправильное фото.
- Кнопки слайдеров кейсов, отзывов и партнеров прокручивают карточки.
- География отображает Яндекс.Карту, а не условную карту России.
- CTA после калькулятора не ломается и не накладывает кнопку на текст.

## 2026-05-07 — Hero calculator focus and catalog proportion tuning

### Что изменилось

- Из первого экрана убраны 4 быстрые категории, чтобы не перегружать шапку выбором разделов.
- Вместо мини-каталога добавлен сильный акцент на калькулятор стоимости как ключевой продукт сайта.
- Спорный складской термин в видимых формулировках заменен на более понятные клиенту: трубы, профиль, сортовой прокат.
- Метрика `1,5 млн м²` сокращена и настроена так, чтобы не переносить `м²` отдельно.
- Подписи метрик в hero выровнены по одному уровню.
- В каталог возвращены метрики `17 разделов`, `4 раздела с фото`, `Клик по карточке`.
- Карточки каталога получили большую зону изображения и более компактный текстовый блок.
- Кнопка/ссылка «Перейти в категорию» выровнена по нижней части карточек.

### Затронутые файлы

- `app/page.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/excelCatalog.ts`
- `README.md`
- `CHANGELOG.md`
- `docs/SITE_PROCESSES.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- В hero больше нет 4 категорий, акцент ведёт в калькулятор.
- В видимом UI нет спорного складского термина, используются трубы, профиль и сортовой прокат.
- Метрики hero не прыгают и `м²` не переносится отдельно.
- В каталоге изображения стали крупнее, а переходы в категорию стоят на одном уровне.

## 2026-05-07 — Hero readability and unified catalog cards

### Что изменилось

- Убран перегруженный блок «быстрый вход в проект» из первого экрана.
- Быстрые переходы в 4 ключевые категории перепакованы в крупные читаемые карточки с короткими подписями.
- Каталог больше не разделён на большие и маленькие карточки: все 17 категорий выведены в единой сетке одинакового размера.
- Удалена служебная фраза про «утвержденную структуру ассортимента».
- Для категорий 5–17 добавлены аккуратные placeholder-визуалы, чтобы карточки выглядели цельно до загрузки реальных фото.
- Вход в категорию теперь работает по клику на всю карточку, а не только по маленькой кнопке.
- Улучшен контраст бейджей, номера категорий отделены от плашек и карточки стало быстрее считывать взглядом.

### Затронутые файлы

- `app/page.tsx`
- `components/LinePageStyles.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Быстрые ссылки на первом экране читаются без долгого всматривания.
- В каталоге все карточки одного размера и собираются в 4 колонки на desktop.
- Переход в раздел работает по клику на любое место карточки.
- Категории 5–17 показывают placeholder, а не случайные маленькие картинки.

## 2026-05-07 — Hero and catalog focus pass

### Что изменилось

- Главная страница усилена по двум приоритетам: первый экран и каталог категорий.
- Hero получил более агрессивную industrial-подачу: продуктовые сигналы, инженерный brief, усиленный контраст и отдельный быстрый вход в каталог.
- Каталог разделен на приоритетные категории и остальной ассортимент, чтобы вход в основные направления был сильнее и понятнее.
- Карточки каталога стали более продуктовыми: добавлены смысловые бейджи, переработана иерархия текста и усилены CTA.
- В проект добавлен отдельный внутренний документ по сборке каталожного контента: роли, структура папок, таблица и правила работы.

### Затронутые файлы

- `app/page.tsx`
- `components/LinePageStyles.tsx`
- `docs/CONTENT_COLLECTION_WORKFLOW.md`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Первый экран выглядит цельно на desktop и mobile.
- Hero сразу объясняет продукт и ведет в каталог или калькулятор.
- Приоритетные категории читаются как главные входы в ассортимент.
- Остальные категории остаются доступными и не теряются визуально.

## 2026-05-06 — Premium visual redesign and public UI cleanup

### Что изменилось

- Первый экран перестроен в стиле сайта по линиям окраски: темный fullscreen-фон, навигация поверх видео/фото, быстрые разделы, крупный заголовок, CTA и метрики.
- Шапка исправлена: больше не закреплена при скролле, логотип на светлой подложке, меню компактнее и без лишних вертикальных полос.
- Каталог выведен полной сеткой из 17 разделов по структуре Excel-листа `Главная`, но без публичных ячеек и внутренних пометок.
- Каталог стал настоящей навигацией: карточки и dropdown ведут на страницы `/catalog/[id]`.
- В категории 1, 3 и 4 добавлены локальные пользовательские фото из `public/assets/images/catalog/`.
- Карточки каталога увеличены и перестилизованы: крупная визуальная зона, больше воздуха, hover-подъем и акцентные подписи.
- Страница категории исправлена в нормальной кодировке, без нечитаемых строк.
- Калькулятор переделан в клиентский трехшаговый конфигуратор: выбор оборудования, понятные параметры, опции, живой расчет и заявка.
- Калькулятор усилен инфографикой, info-подсказками и более контрастными состояниями.
- CTA-баннер бесплатного расчета перепакован, чтобы текст не ломался узкой колонкой.
- География получила SVG-силуэт России вместо абстрактной фигуры, Яндекс.Карта переведена на поиск по адресу.
- Убраны публичные упоминания Excel, адресов ячеек, служебных листов и сокращений вроде `Авт. скл. лист. металл`.
- Добавлены визуальные блоки: что можно хранить, до/после склада, кейсы-слайдер, интерактивная география, отзывы-слайдер, преимущества с иконками.
- Усилены телефоны в шапке: две строки, иконка телефона, социальные кнопки и hover/micro-motion.
- Обновлены данные калькулятора и расчетные подписи в нормальной кодировке.
- Обновлены `README.md` и handoff-документация.

### Затронутые файлы

- `app/page.tsx`
- `app/catalog/[id]/page.tsx`
- `public/assets/images/catalog/01-auto-sheet-metal.jpg`
- `public/assets/images/catalog/03-sort-and-pipe-storage.jpg`
- `public/assets/images/catalog/04-manual-sort-and-pipe-storage.webp`
- `public/assets/images/README.md`
- `.vercelignore`
- `app/layout.tsx`
- `components/BrandMark.tsx`
- `components/Calculator.tsx`
- `components/LeadForm.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/excelCatalog.ts`
- `data/storageSystems/excelCalculator.ts`
- `lib/calculator/pricing.ts`
- `tests/calculator.test.ts`
- `README.md`
- `docs/CLAUDE_CODE_HANDOFF.md`
- `docs/SITE_PROCESSES.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- На сайте нет публичных слов `Excel`, `Главная!`, `Авт.скл`, `печь полимеризации`, `линия порошковой окраски`.
- Hero-фон занимает всю шапку.
- Dropdown «Каталог» показывает человеческие названия разделов.
- Каталог содержит 17 разделов.
- Калькулятор показывает цену `от ... ₽`, а не точный КП или диапазон.
- `/api/leads` отвечает в mock-режиме.

## 2026-05-06 — Strict Excel catalog and calculator

### Что изменилось

- Каталог на главной синхронизирован со структурой листа `Главная`.
- Добавлены `data/storageSystems/excelCatalog.ts` и `data/storageSystems/excelCalculator.ts`.
- Калькулятор переведен с произвольных полей на фиксированные варианты: высота, ширина, длина, нагрузка, полки, башни и опции.
- Расчет показывает стартовую сумму `от ... ₽`.
- README стал актуальной картой проекта, а не дневником мелких правок.
- Добавлен handoff-файл для Claude Code.

### Затронутые файлы

- `app/page.tsx`
- `components/Calculator.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/excelCatalog.ts`
- `data/storageSystems/excelCalculator.ts`
- `lib/calculator/pricing.ts`
- `tests/calculator.test.ts`
- `README.md`
- `docs/CLAUDE_CODE_HANDOFF.md`
- `CHANGELOG.md`

### Что проверить

- Каталог не содержит разделов сверх листа `Главная`.
- Калькулятор использует только заложенные значения.
- Старые следы про печь и линии окраски не выводятся в UI.
- `npm run lint`, `npm run test`, `npm run build`.

## 2026-04-28 — Structure aligned with powder coating reference

### Что изменилось

- Главная страница перестроена по последовательности блоков сайта `линииокраски.рф`.
- Добавлены блоки: hero, каталог, калькулятор, CTA-баннеры, кейсы, география, отзывы, о компании, преимущества, 3 шага, партнеры, FAQ, контакты и футер.
- Добавлена форма `components/LeadForm.tsx`, отправляющая данные в `/api/leads`.

### Затронутые файлы

- `app/page.tsx`
- `components/LeadForm.tsx`
- `components/LinePageStyles.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- Структура блоков совпадает с заданной последовательностью.
- Формы связи и калькулятор отправляют данные в `/api/leads`.
- Контакты, карта и футер отображаются.

## 2026-04-28 — Industrial MVP release

### Что изменилось

- Создан Next.js/React MVP сайта «Системы хранения металла».
- Добавлен логотип КБ Парус.
- Добавлены базовые данные `/data/storageSystems`, расчетная логика `/lib/calculator`, endpoint `/api/leads` и unit-тесты.
- Проект задеплоен в существующий Vercel project `kbparus-metal-storage`.

### Затронутые файлы

- `app/`
- `components/`
- `data/storageSystems/`
- `lib/calculator/`
- `tests/calculator.test.ts`
- `public/brand/logo-g.png`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- Главная открывается.
- Калькулятор работает.
- `/api/leads` отвечает `200` в mock-режиме.
- Production URL: https://kbparus-metal-storage.vercel.app
