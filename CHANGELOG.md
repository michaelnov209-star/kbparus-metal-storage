# CHANGELOG

## 2026-05-06 — Strict Excel catalog and calculator

### Что изменилось

- Каталог на главной теперь строится строго по видимым ячейкам листа `Главная` из `Калькулятор-New.xls`.
- Добавлен файл `data/storageSystems/excelCatalog.ts` с пунктами каталога, исправлением явных опечаток и ссылками на исходные ячейки Excel.
- Добавлен файл `data/storageSystems/excelCalculator.ts` с Excel-профилями расчёта, фиксированными вариантами Д×Ш×В, нагрузок, полок, башен и опций.
- Калькулятор переделан из формы со свободными полями в конфигуратор с выбором готовых Excel-значений.
- Расчёт в `lib/calculator/pricing.ts` сверяется с 6 формульными листами Excel и показывает сумму в формате `от ... ₽`.
- Hero и шапка переведены в светлую промышленную тему: большой заголовок, фото/video-placeholder, метрики, dropdown «Каталог», два телефона сразу.
- Удалён старый правый hero-panel, из-за которого первый экран выглядел тяжёлым.
- README обновлён как актуальная карта проекта.
- Добавлен handoff-файл для Claude Code.

### Затронутые файлы

- `app/page.tsx`
- `components/Calculator.tsx`
- `components/LinePageStyles.tsx`
- `data/storageSystems/excelCatalog.ts`
- `data/storageSystems/excelCalculator.ts`
- `lib/calculator/pricing.ts`
- `lib/calculator/types.ts`
- `lib/calculator/validation.ts`
- `tests/calculator.test.ts`
- `README.md`
- `docs/CLAUDE_CODE_HANDOFF.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- На главной в каталоге нет разделов, которых нет на листе `Главная`.
- В шапке видно оба телефона.
- Dropdown «Каталог» открывается и содержит Excel-пункты.
- Калькулятор использует фиксированные значения, а не произвольные поля.
- В интерфейсе нет слов про печь полимеризации и линии окраски.
- `/api/leads` отвечает в mock-режиме.

## 2026-05-06 — Structure aligned with powder coating reference

### Что изменилось

- Главная страница перестроена по структуре `линииокраски.рф`.
- Добавлены/пересобраны блоки: hero с видео-фоном, каталог оборудования, калькулятор стоимости, CTA-баннеры, кейсы, география поставок, отзывы со slider-like лентой, блок о компании с метриками, баннер на `kbparus.ru`, преимущества, 3 шага отгрузки, партнёры, FAQ, форма связи, контакты, карта и футер.
- Добавлена клиентская форма `components/LeadForm.tsx`, отправляющая заявки в существующий `/api/leads`.
- Визуальный слой вынесен в классы `.line-*`, чтобы сохранить существующую архитектуру и не трогать сторонние незакоммиченные файлы.
- Сохранены `data/storageSystems`, `lib/calculator`, `/api/leads`, готовность под CMS, Bitrix24 и 1С.

### Затронутые файлы и папки

- `app/page.tsx`
- `app/globals.css`
- `components/LeadForm.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Hero выглядит как шапка с видео/анимированным фоном.
- Структура блоков совпадает с заданной последовательностью.
- Форма связи и калькулятор отправляют данные в `/api/leads`.
- Контактный блок содержит телефоны, email, адрес, график и карту.

## 2026-04-28 — Full premium product redesign

### Что изменилось

- Выполнен полный visual/product redesign без изменения базовой архитектуры проекта.
- Главная страница пересобрана как цельный B2B-продуктовый маршрут: hero → проблема → объект хранения → решения → до/после → конфигуратор → проект → преимущества → контакты.
- Hero заменён на premium industrial-сцену с крупной типографикой, фотореалистичным фоном, product visual-layer, метриками и сильными CTA.
- Калькулятор визуально усилен до инженерного конфигуратора: живой summary, анимированная стартовая сумма «от», визуальное превью конфигурации, карточки выбора и скрытые advanced-поля.
- Обновлена визуальная система: тёмные premium-секции, крупные фото, плотные, но спокойные карточки, более дорогие отступы, радиусы и hover-эффекты.
- Сохранены `data/storageSystems`, `lib/calculator`, `/api/leads`, готовность под CMS, Bitrix24 и 1С.

### Затронутые файлы и папки

- `app/page.tsx`
- `app/globals.css`
- `components/HeroVisual.tsx`
- `components/Calculator.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Первый экран выглядит как дорогой промышленный продукт.
- Калькулятор воспринимается как конфигуратор, а не форма.
- В интерфейсе по-прежнему показывается сумма «от …», без обещания точной цены.
- `/api/leads` отвечает `200` в mock-режиме.

## 2026-04-28 — Visual simplification and real photos

### Что изменилось

- SVG-заглушки в ключевых блоках заменены на реальные временные remote JPEG-фотографии из Pexels.
- Упрощена публичная структура: удалён повторяющийся тяжёлый каталог оборудования, сокращена навигация, в блоке решений оставлены основные карточки.
- Hero получил тёмный фотореалистичный промышленный фон, чтобы первый экран воспринимался как реальный производственный продукт.
- Калькулятор стал легче для клиента: основные поля оставлены на виду, инженерные поля башен, полок, кассет и габаритов системы спрятаны в раскрываемый блок.
- README обновлён под актуальное состояние визуалов и структуры.

### Затронутые файлы и папки

- `data/storageSystems/visualAssets.ts`
- `app/page.tsx`
- `app/globals.css`
- `components/Calculator.tsx`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- На production URL видны реальные фотографии, а не только SVG/alt-подписи.
- Калькулятор выглядит проще: сложные поля доступны через раскрываемый блок.
- `/api/leads` продолжает отвечать `200` в mock-режиме.

## 2026-04-28 — Excel calculator alignment

### Что изменилось

- Калькулятор повторно сверён с формульной структурой `Калькулятор New.xlsx`.
- Расчёт в `lib/calculator/pricing.ts` перестроен ближе к Excel: отдельно считаются коэффициенты габаритов, нагрузки, башни, полки/кассеты, выкатные секции, автоматизированный модуль и опции.
- Из публичного интерфейса убран ценовой диапазон. Вместо него сайт показывает стартовую сумму в формате «от …», рассчитанную модулем калькулятора.
- API `/api/leads` теперь передаёт `UF_PRELIMINARY_PRICE_FROM` и структурированный расчёт без `min/max` диапазона.
- Обновлены unit-тесты и документация под новую модель цены.

### Затронутые файлы и папки

- `lib/calculator/pricing.ts`
- `lib/calculator/types.ts`
- `components/Calculator.tsx`
- `app/api/leads/route.ts`
- `data/storageSystems/content.ts`
- `data/storageSystems/priceFactors.ts`
- `tests/calculator.test.ts`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- В калькуляторе отображается «от …», а не диапазон.
- При изменении габаритов, нагрузки, башен, полок и кассет сумма пересчитывается.
- `/api/leads` отвечает `200` в mock-режиме и содержит поле `UF_PRELIMINARY_PRICE_FROM`.

## 2026-04-28 — Industrial MVP release

### Что изменилось

- Создан Next.js/React MVP сайта «Системы хранения металла» для КБ Парус.
- Добавлен промышленный fullscreen hero с pseudo-video визуалом.
- Подключён реальный логотип КБ Парус.
- Добавлены основные продуктовые блоки: проблемы склада, что можно хранить, решения, до/после, как это работает, карточки оборудования, проектирование, преимущества, сценарии применения, данные для инженера и FAQ.
- Калькулятор переделан в многошаговый конфигуратор с прогрессом, карточками выбора, live summary, предварительным диапазоном цены и отправкой заявки.
- Добавлен API `/api/leads` с mock-режимом и подготовкой под Bitrix24.
- Добавлена структура данных под CMS и 1С.
- Добавлены favicon, иконки `lucide-react`, папка для будущих изображений и визуальные placeholder-изображения.
- Проект задеплоен в существующий Vercel project `kbparus-metal-storage`.

### Затронутые файлы и папки

- `app/`
- `components/`
- `data/storageSystems/`
- `lib/calculator/`
- `public/brand/logo-g.png`
- `public/assets/images/README.md`
- `tests/calculator.test.ts`
- `package.json`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run lint`
- `npm run test`
- `npm run build`
- Главная страница открывается.
- В шапке и футере отображается логотип.
- На сайте нет публичного блока конкурентных референсов.
- Калькулятор проходит шаги и показывает предварительный диапазон.
- `/api/leads` отвечает `200` в mock-режиме.
- Production URL открывается: https://kbparus-metal-storage.vercel.app

### Известные ограничения

- GitHub repository не создан из текущей среды: нет `gh`, GitHub token или авторизованного remote.
- Визуалы пока временные и должны быть заменены на реальные фото/видео КБ Парус.
- Bitrix24 webhook не подключён в окружении production.

## 2026-04-28 — Visible local imagery update

### Что изменилось

- Добавлены локальные SVG-изображения в `public/assets/images`, чтобы сайт не зависел от внешних CDN и реально показывал визуалы на production.
- Обновлена карта изображений `data/storageSystems/visualAssets.ts`.
- Hero, блок «что можно хранить», карточки решений, каталог оборудования, «до/после» и калькулятор используют реальные `<img>` с локальными файлами.

### Затронутые файлы и папки

- `public/assets/images/`
- `data/storageSystems/visualAssets.ts`
- `app/page.tsx`
- `components/Calculator.tsx`
- `app/globals.css`
- `README.md`
- `CHANGELOG.md`

### Что проверить

- `npm run build`
- Production URL открывается.
- В DOM есть изображения из `/assets/images/*.svg`.
- В браузере у изображений `naturalWidth > 0`.
- Блоки hero, решения, каталог, до/после и калькулятор визуально не пустые.
